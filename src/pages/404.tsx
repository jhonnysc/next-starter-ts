import { NotFoundContainer, Title } from '@/styles/pages/404';

const NotFound = () => {
  return (
    <NotFoundContainer>
      <Title>Not Found...</Title>
    </NotFoundContainer>
  );
};

export default NotFound;
